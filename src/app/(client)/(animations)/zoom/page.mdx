import previewBlocks from "@/lib/previewBlocks";
import { zoomMd } from "@/lib/metadata/metadata"
import GeneratePageAnimations from "@/components/layouts/maincontent/GeneratePageAnimations";

export const metadata = {
    title: zoomMd.title,
    description: zoomMd.description
}

# Zoom Animations

<GeneratePageAnimations animationCategory="zoom" />
